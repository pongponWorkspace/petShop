// Temporary wrapper until full refactor is complete
import React from 'react';
// Import the original screen for now
const MyCouponsScreen = React.lazy(() => import('../../app/my-coupons'));

export default MyCouponsScreen;